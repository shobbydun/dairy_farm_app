import 'package:flutter/material.dart';

class HeatDetectionPage extends StatelessWidget {
  const HeatDetectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}