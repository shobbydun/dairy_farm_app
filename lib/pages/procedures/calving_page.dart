import 'package:flutter/material.dart';

class CalvingPage extends StatelessWidget {
  const CalvingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}