import 'package:flutter/material.dart';

class DehorningPage extends StatelessWidget {
  const DehorningPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}